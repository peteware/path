# -*- python -*-
import os
for dir in ['src', os.path.join('tests', 'pathtest')]:
    SConscript(dirs = dir, 
           build_dir = os.path.join('build', 'Debug', dir), duplicate = 0)
#'tests/pathtest/SConscrtipt',
